SpaceClaim Construct add-in

Documentation WIP

Possibly missing AESCConstruct25.xml file when built. If so, add to place where .dll is built

```
<?xml version="1.0" encoding="utf-8" ?>
<AddIns>
  <AddIn
    name="AESC Construct 25"
    description="A custom add-in for SpaceClaim."
    assembly="AESCConstruct25.dll"
    typename="AESCConstruct25.Construct25"
    host="NewAppDomain" />
</AddIns>
```
