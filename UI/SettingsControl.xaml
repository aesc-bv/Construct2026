<UserControl x:Class="AESCConstruct25.UI.SettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="400" Background="White"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/AESCConstruct25;component/Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Title -->
            <RowDefinition Height="Auto"/>
            <!-- Part naming -->
            <RowDefinition Height="*"/>
            <!-- DataGrid -->
            <RowDefinition Height="Auto"/>
            <!-- Save button -->
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Style="{StaticResource TitleTextStyle}"
                   Text="Settings"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"/>

        <!-- Part naming -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10" VerticalAlignment="Center">
            <TextBlock Text="Part naming:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="PartNameTextBox" Width="200" Height="30"/>
        </StackPanel>

        <!-- Types/Names grid -->
        <DataGrid Grid.Row="2"
                  x:Name="TypesDataGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="True"
                  CanUserDeleteRows="True"
                  HeadersVisibility="Column"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Type"
                                    Binding="{Binding Type, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    Width="*" />
                <DataGridTextColumn Header="Name"
                                    Binding="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Save button -->
        <Button Grid.Row="3"
                x:Name="SaveButton"
                Content="Save"
                Style="{StaticResource FooterButtonStyle}"
                HorizontalAlignment="Center"
                Margin="0,10,0,0"
                Click="SaveButton_Click"/>
    </Grid>
</UserControl>
